import { ShowPassDirective } from './show-pass.directive';

describe('ShowPassDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowPassDirective();
    expect(directive).toBeTruthy();
  });
});
